node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# This project uses npm. A bun lockfile makes hosts autodetect bun and
# generate a build command that never runs the app.
bun.lock
bun.lockb
